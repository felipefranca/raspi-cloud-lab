name: CI Pipeline
on:
  push:
    branches:
      - master
jobs:
  build:
    environment: cloud-learn-secrets
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - name: Build and Clean With Maven
        run: mvn clean package -DskipTests
      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Setup Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and Push Multi-Arch Image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            felipejofranca/cloud-learn:latest
            felipejofranca/cloud-learn:${{ github.sha }}
          platforms: linux/amd64,linux/arm64

  deploy:
    name: Deploy on Raspberry
    needs: build
    runs-on: [self-hosted, linux, arm64]
    steps:
      - name: Pull immutable image tag
        run: docker pull felipejofranca/cloud-learn:${{ github.sha }}

      - name: Stop and remove previous container
        run: |
          docker stop cloud-learn || true
          docker rm cloud-learn || true

      - name: Run updated container
        run: |
          docker run -d \
            --name cloud-learn \
            --restart unless-stopped \
            --network cloud-network \
            -p 8080:8080 \
            -e spring.datasource.url="jdbc:postgresql://postgres:5432/appdb" \
            -e spring.datasource.username="admin" \
            -e spring.datasource.password="admin" \
            felipejofranca/cloud-learn:${{ github.sha }}
